## File naming

---

- kebab-case
- The file name reflects its purpose
- One file — one responsibility
- For types and interfaces, add a suffix indicating the type (e.g., `user.type.ts`, `user.interface.ts`)
